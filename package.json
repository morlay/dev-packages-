{
  "private": true,
  "license": "WTFPL",
  "engines": {
    "npm": ">= 3"
  },
  "scripts": {
    "ncu": "lerna exec -- ncu -u",
    "lint": "lerna exec -- eslint src/ --fix",
    "test": "npm run lint",
    "release": "lerna publish --skip-git"
  },
  "devDependencies": {
    "@morlay/babel-presets": "0.3.x",
    "@morlay/eslint-config": "0.3.x",
    "npm-check-updates": "2.7.x",
    "lerna": "2.0.0-beta.24"
  }
}
