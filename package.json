{
  "private": true,
  "license": "WTFPL",
  "engines": {
    "npm": ">= 3"
  },
  "scripts": {
    "ncu": "lerna exec -- ncu -a",
    "lint": "lerna exec -- eslint ./ --fix",
    "mocha": "mocha -R dot --compilers .:babel-core/register packages/**/__tests__/*.spec.js",
    "test": "npm run lint && npm run mocha",
    "pre-release": "lerna publish --skip-git --skip-npm",
    "release": "lerna exec -- npm publish --access public"
  },
  "devDependencies": {
    "@morlay/babel-preset": "1.0.x",
    "@morlay/eslint-config": "1.0.x",
    "@morlay/tests": "1.0.x",
    "babel-plugin-transform-es2015-modules-commonjs": "6.11.x",
    "lerna": "2.0.0-beta.24",
    "npm-check-updates": "2.8.x"
  }
}
