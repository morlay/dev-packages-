{
  "private": true,
  "license": "WTFPL",
  "engines": {
    "npm": ">= 3"
  },
  "scripts": {
    "ncu": "lerna exec -- ncu -u",
    "lint": "lerna exec -- eslint ./ --fix",
    "test": "npm run lint",
    "pre-release": "lerna publish --skip-git --skip-npm",
    "release": "lerna exec -- npm publish --access public"
  },
  "devDependencies": {
    "@morlay/eslint-config": "1.0.x",
    "npm-check-updates": "2.7.x",
    "lerna": "2.0.0-beta.24"
  }
}
